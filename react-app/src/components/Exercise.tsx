import React from "react";

const Exercise = () => {
  return (
    <div>
      <button type="button" className="btn btn-primary">
        Button
      </button>
    </div>
  );
};

export default Exercise;
